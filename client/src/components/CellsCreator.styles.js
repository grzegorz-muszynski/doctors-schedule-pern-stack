let myRed = 'orange';
// let myRed = 'hsl(0, 100%, 48%)';
let oldColor = 'hsl(180, 100%, 21%)';

const bookedSlotRed = {
    backgroundColor: myRed,
    paddingLeft: '8px',
    paddingTop: '2px',
}

const bookedSlotOld = {
    backgroundColor: oldColor,
    paddingLeft: '8px',
    paddingTop: '2px',
}

export const styles = {
    bookedSlotRed: bookedSlotRed,
    bookedSlotOld: bookedSlotOld
}